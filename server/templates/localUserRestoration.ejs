<%
    const title = 'LightweightProject - User account Restoration';
    const description = 'LightweightProject - User account Restoration';
    const keywords = 'LightweightProject, account, restoration, authentication';
    const pageTitle = 'User account Restoration';
    const additionalStyles = '';
    const body = `
        <p class="greeting-note">Hello <b>${username}</b> !<br>
           We received a request to restore your LightweightProject account.<br>
           Click the button below to complete the restoration process:
        </p>
        <div class="button-container">
            <a href="${callbackUrl}"
               class="button"
               title="LightweightProject - account restoration link"
               target="_blank">Restore Account</a>
        </div>
        <p class="expiry-note">This link will expire in 15 minutes for security reasons.</p>
        <p class="info-note">If you didn't request this account restoration, you can safely ignore this email.<br>
            <i>Need help? Contact our support team for assistance.</i>
        </p>
    `;
%>
<%- include('layouts/email-layout.ejs', { title, description, keywords, pageTitle, additionalStyles, body }) %>
